# ------------------------
# Clojure Project Git Ignore file patterns
#
# Ignore all except patterns starting with !
# Add comments on separate lines, not same line as pattern
# ------------------------

# ------------------------
# Ignore everthing in root directory
/*


# Custom stuff added by David
# this file is autogenerated by jflex
!xic
!xic-build
# everything in the project root is ignored by default, so there's no need
# to explicitly ignore XiLexer.java, because it's the only file in the java/
# directory currently. If I end up adding more java source files to java/,
# then it would make sense to explicitly unignore java/ and ignore this
# particular source file.
#java/XiLexer.java
!e2e/
e2e/xthScript.results


# ------------------------
# Common project files
!CHANGELOG.md
!README.md
!LICENSE

# ------------------------
# Include Clojure project & config
!build.clj
!deps.edn
!pom.xml
!dev/
!docs/
!resources/
!src/
!test/

# ------------------------
# Include Clojure tools
!.cljstyle
!.dir-locals.el
!compose.yaml
!Dockerfile
!.dockerignore
!Justfile
!tests.edn

# ------------------------
# Include Git & CI workflow
!.gitattributes
!.gitignore
!.github/

# ------------------------
# Include ClojureScript Figwheel
!figwheel-main.edn
!*.cljs.edn
